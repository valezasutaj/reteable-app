import { Routes } from '@angular/router';
import { RateablesComponent } from './components/rateables/rateables.component';

export const routes: Routes = [
    { path: '', component: RateablesComponent }
];
